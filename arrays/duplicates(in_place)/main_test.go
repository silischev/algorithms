package main

import "testing"

func TestDuplicates(t *testing.T) {
	/*cases := []struct {
		act []int
		exp []int
	}{
		{[]int{1, 1, 2}, []int{1, 2}},
	}

	for _, c := range cases {
		act := removeDuplicates(c.act)
		if act != c.result {
			t.Fatalf("Failed result [%t] for [%s], [%s]", act, c.s1, c.s2)
		}
	}*/
}
